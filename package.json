{
  "private": true,
  "type": "commonjs",
  "scripts": {
    "seed": "node scripts/seed.js",
    "test": "node --test scripts/*.test.js test/firestore.rules.contract.test.js",
    "test:rules:emulator": "firebase emulators:exec --only firestore --project demo-dazhugong \"node --test test/firestore.rules.emulator.test.js\""
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.18.0",
    "firebase-tools": "^15.28.1"
  }
}
